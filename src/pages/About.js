import React from "react"

const About = () => {
  const style = {
    margin: '50px',
    textAlign: 'center',
    fontSize: '72px',
  };
  return (
    <div style={style}>Hello from the about page!</div>
  );
}

export default About
